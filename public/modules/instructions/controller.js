"use strict";
angular.module("instructions")
shopifyApp.controller("instructionsCtrl", ['$window', '$scope', '$rootScope', '$localStorage', 'instructionsService','$localStorage', function($window, $scope, $rootScope, $localStorage, instructionsService) {
		}]);